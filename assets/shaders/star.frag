// Fragment shader (e.g., star.frag)
#version 450

out vec4 frag_color;

void main() {
    // Generate star colors based on position or other criteria
    // For simplicity, this example just outputs white color for all pixels
    frag_color = vec4(1.0, 1.0, 1.0, 1.0);
}
